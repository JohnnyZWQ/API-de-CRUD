
- @Entity:  Marca uma classe como uma entidade que será mapeada para uma tabela no banco.

- @GeneratedValue(generator = "UUID"):  Gera automaticamente um ID único do tipo UUID para o campo anotado com `@Id`.

- @RestController:  Transforma a classe em um controlador REST, retornando os dados no formato JSON.

- @RequestMapping("/"): Define o endpoint base para todas as requisições deste controlador.

- `@PostMapping`: Envia dados para criação (POST)
- `@GetMapping`: Busca informações (GET)
- `@PutMapping`: Atualização (PUT)
- `@DeleteMapping`: Exclusão (DELETE)

- @SpringBootApplication:  É uma combinação de `@Configuration`, `@EnableAutoConfiguration` e `@ComponentScan`. 
Diz ao Spring para configurar a aplicação automaticamente.

- H2 Database:   

O H2 é um banco de dados em memória usado para testes em aplicações Spring Boot. 

- Método `main()` na classe principal:  Inicia a aplicação com `SpringApplication.run()`.

- JPA:

JPA (Java Persistence API) é uma especificação do Java para trabalhar com banco de dados usando objetos Java.

- JpaRepository:   

É uma interface do Spring Data que fornece métodos prontos para acessar o banco, como `findAll()`, `save()`, `deleteById()`.

- Maven:  

Maven é um gerenciador de dependências.

- BCrypt:

É uma biblioteca para criptografar senhas com segurança.

- FilterAuth:

Intercepta requisições no endpoint definido e verifica se o usuário está autenticado via cabeçalho `Authorization`.

- HttpStatus.OK (200):

🔹 200 OK = Requisição bem-sucedida

- HttpStatus.BAD_REQUEST (400):

Erro na requisição

- response.sendError(401, ...)`?  

Rejeita a requisição por falha na autenticação.  

- HttpServletRequest:   Ele contém informações da requisição HTTP, como headers, corpo e parâmetros.  

- HttpServletResponse:     Ele representa a resposta do servidor, permitindo definir status HTTP, headers e corpo da resposta.  

POSTMAN:

O Postman é uma ferramenta usada para testar APIs REST, 
permitindo enviar requisições HTTP (GET, POST, PUT, DELETE) e visualizar as respostas.  

- Como enviar um corpo (`body`) em uma requisição POST no Postman?  

{
  "name": "João",
  "email": "joao@email.com",
  "password": "123456"
}